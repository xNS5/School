Fix conditional statement such that it actually checks for whether the
number is a prime or not -- COMPLETED

implement file input and output -- COMPLETED INPUT AND OUTPUT
Solution to write to text file:
    Needed #:mode and #:exists flags in the open-output-file

Issue with printing to text file:
For whatever reason, if I iterated from zero it printed all of the numbers
in reverse.

Solution: 
    Instead of iterating from zero, iterate from the top (or y)

Fix two squares function -- COMPLETED

Issue: I don't know if I should drop #lang r5rs at the top of the file. My program gives me errors when I
it
